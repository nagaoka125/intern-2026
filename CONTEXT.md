# 視聴画面

配信視聴ページ（動画視聴・リアルタイムコメント・アイテム送信）のドメイン用語集。

## Language

**アイテム**:
視聴者がコメントと同時に配信者へ送れるギフト。`id` / `name` / `iconUrl` / コスト / グループ / アニメーション有無 を持つ。アイテムサーバー（`ITEMS_URL`）が一覧を提供する。

**コスト**:
アイテムに設定された数値。実際の決済通貨ではなく画面表示上の値で、「円」表記で表示する。現在のデータでは 10 / 50 / 150 / 400 / 1000 の5段階のみが存在する。
_Avoid_: 金額、価格

**コスト帯**:
アイテムをコストで絞り込むための3区分フィルタ: 〜100 / 100〜500 / 500〜。データ上の5段階のコストがちょうどこの3区分に分かれる。

**グループ**:
アイテムが属するカテゴリ（例: 気持ち、自然、食べ物、お祝い）。アイテムサーバー側が定義するため、クライアントはハードコードせず取得済みのアイテム一覧から動的に選択肢を組み立てる。

**アニメーション付きアイテム**:
`animationUrl` が設定されたアイテム。通常のアイコンとは別の、アニメーション再生される画像（webp）を持つ。コメント表示領域にこのアイテムを含むコメントが表示された際にのみ、通常のアイコンに加えてこの画像を大きく表示する。アイテムパネル（選択用の一覧）では通常のアイコンのみを表示し、アニメーションは表示しない。

**アイテムパネル**:
送信領域内でボタンにより開閉するアイテム一覧とフィルタのUI。デフォルトは閉じた状態で、開閉はスライドダウンのアニメーションを伴う。

**コスト強度**:
アイテムのコストを 0〜1 の範囲に正規化した値。実データの5段階（10/50/150/400/1000）に直接依存せず、余裕を持たせた固定の基準範囲（アイテムサーバー側で将来コスト値が増減しても破綻しないようにするため）を対数スケールで正規化する。コメント表示領域における演出（背景色の彩度・明度、登場モーションの大きさ、発光エフェクトの強さ）は、すべてこの1つの値から一貫して導出する。

**コスト強度演出**:
コメント表示領域でアイテム付きコメントが追加された際に発生する、コスト強度に連動した視覚効果。背景色・登場時のポップモーション・発光（グロー）の強さがコスト強度に応じて変化する。アニメーション有無に関わらず、アイテムが添付された全てのコメントに適用される（アイテム未添付の通常コメントには適用しない）。アニメーション付きアイテムの大きな画像表示（`animationUrl` による webp 再生）とは独立した仕組みであり、両者は同時に発生し得る。

### アイテム獲得（抽選）

**所持数**:
視聴者がアイテムごとに持っている数量。初めて存在を知ったアイテムは所持数10からスタートし、上限はない。送信に成功すると選択したアイテムの所持数が1減り、抽選に当選すると当選したアイテムの所持数が1増える（いずれも成功時のみ増減し、失敗時は変化しない）。所持数が0のアイテムはアイテムパネルで選択できない。サーバー側にユーザーを識別する仕組みがないため、各ブラウザの `localStorage` にのみ保存され、他の端末・ブラウザとは共有されない。
_Avoid_: 在庫、所有数

**ポイント**:
コメントやアイテムの送信に成功するたびに獲得できる、抽選のために貯める数値。通常コメント（アイテムなし）の送信で1pt、アイテム付き送信ではアイテムのコスト帯に応じた数のポイントを獲得する（この場合、基本の1ptは加算されない）。所持数と同様に送信成功時のみ加算され、`localStorage` にのみ保存される。
_Avoid_: pt、スコア

**抽選**:
ポイントを100pt消費して1回行える、アイテムを1つランダムに獲得する操作。消費後に余ったポイントは繰り越される。まずコスト帯単位の重み付け（〜100円 65% / 100〜500円 27% / 500円〜 8%）でどのコスト帯から出すかを決め、その帯に属するアイテムの中から均等な確率で1つを選ぶという2段階の抽選になっている。取得済みの全アイテムが対象で、除外されるものはない。
_Avoid_: ガチャ

**抽選演出**:
ヘッダーの抽選ボタンを押した際に開くモーダルで再生される、当選アイテムをスロットのように見せる演出。アイテムアイコンが横スクロールしながら減速し、停止した位置のアイテムが当選アイテムとして強調表示される。

### 視聴統計カウンター

**総コメント数**:
視聴画面がコメントサーバーとSSE接続を確立してから受信した送信イベントの累計件数。テキストのみ・アイテムのみ・両方を含む送信のいずれも、受信1件につき1件としてカウントする。コメントサーバーに過去ログ取得手段がないため、ページを再読み込みすると0から数え直しになる（[ADR 0006](docs/adr/0006-viewer-stats-are-session-scoped.md) 参照）。

**アイテム数（カウンター）**:
総コメント数と同じ集計期間のうち、アイテムが添付されていた送信イベントだけを数えるカウンター。総コメント数の内数（サブセット）であり、アイテムパネルに並ぶ「アイテムの種類数」とは別の値。
_Avoid_: アイテム総数、送信数

### 連続送信防止

**送信クールダウン**:
送信のたびに一定時間（3秒）だけ送信ボタンを無効化する、1回ごとの連投防止の仕組み。

**連続送信ロック**:
直近10秒以内に3回送信した場合に、送信クールダウンとは別に20秒間送信そのものを禁止する仕組み。短時間の連打によるコメント欄の荒れやポイントの稼ぎすぎを防ぐためのもの。
